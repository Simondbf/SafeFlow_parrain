import 'package:flutter/material.dart';

class FilleulsScreen extends StatelessWidget {
  const FilleulsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Filleuls')),
      body: const Center(child: Text('Vue détaillée des filleuls (à implémenter)')),
    );
  }
}

