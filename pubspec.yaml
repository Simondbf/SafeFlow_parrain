name: safeflow_parrain
description: SafeFlow - Dashboard Parrain
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1   # Provider + state management recommandé
  go_router: ^14.2.7         # Navigation déclarative, ShellRoute pour BottomNav persistante
  # firebase_core: ^2.10.0   # Décommenter si vous intégrez Firebase
  # cloud_firestore: ^4.8.0  # Décommenter si vous utilisez Firestore

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
